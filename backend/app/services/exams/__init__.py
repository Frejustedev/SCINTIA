"""Per-exam analyzers (strategy pattern).

Each exam type implements the common ``ExamAnalyzer`` interface so a new exam is
added as a class without touching the rest (docs/02_ARCHITECTURE.md §3).
"""
